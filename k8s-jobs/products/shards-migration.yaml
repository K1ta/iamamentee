apiVersion: batch/v1
kind: Job
metadata:
  name: shards-migration
  namespace: products
spec:
  backoffLimit: 3
  template:
    spec:
      containers:
        - name: products
          image: docker.io/library/products:latest
          imagePullPolicy: Never
          command: ["./products", "migrate-shards"]
          env:
            - name: MIGRATOR_DB_CONNECTIONS
              value: "postgres-0>postgresql://admin:password@postgres-0.postgres.products-infra:5432/products?sslmode=disable,postgres-1>postgresql://admin:password@postgres-1.postgres.products-infra:5432/products?sslmode=disable"
            - name: MIGRATOR_SHARDS
              value: "products-0:postgres-0"
            - name: MIGRATOR_PREV_SHARDS
              value: "products-0:postgres-0,products-1:postgres-1"
            - name: MIGRATOR_PREV_SHARDS_START_FROM
              value: ""
            - name: MIGRATOR_BATCH_LIMIT
              value: "1"
      restartPolicy: Never
