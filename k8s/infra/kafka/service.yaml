apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: infra
  labels:
    app: kafka
spec:
  clusterIP: None # Headless service
  ports:
    - port: 9092
      name: client
    - port: 9093
      name: interbroker
  selector:
    app: kafka
